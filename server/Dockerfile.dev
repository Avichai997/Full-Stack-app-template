FROM node:22-alpine

WORKDIR /app

COPY package*.json ./

RUN npm install

COPY . .

EXPOSE 3001

# Wait for database to be ready, then run migrations (which includes db:create), then start dev server
CMD sh -c 'echo "Waiting for database to be ready..." && sleep 3 && npm run migration:run && npm run start:dev'

